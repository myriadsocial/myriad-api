version: '3'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/home/myriad/app
      - /home/myriad/app/node_modules
      - /home/myriad/app/dist
    environment:
      - HOST=0.0.0.0
      - PORT=3001
    ports:
      - 3001:3001
