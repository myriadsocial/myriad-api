export enum OrderFieldType {
  COMMENT = 'comment',
  COUNT = 'count',
  LATEST = 'latest',
  POPULAR = 'popular',
  TIP = 'tip',
  UPDATEDAT = 'updatedAt',
  UPVOTE = 'upvote',
}

export enum OrderType {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum TimelineType {
  ALL = 'all',
  EXPERIENCE = 'experience',
  FRIEND = 'friend',
  TRENDING = 'trending',
}
