export enum OrderFieldType {
  COMMENT = 'comment',
  COUNT = 'count',
  NAME = 'name',
  LATEST = 'latest',
  POPULAR = 'popular',
  TIP = 'tip',
  UPDATEDAT = 'updatedAt',
  UPVOTE = 'upvote',
  USERNAME = 'username',
}

export enum OrderType {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum TimelineType {
  ALL = 'all',
  EXPERIENCE = 'experience',
  FRIEND = 'friend',
  TRENDING = 'trending',
  PROFILE = 'profile',
}
